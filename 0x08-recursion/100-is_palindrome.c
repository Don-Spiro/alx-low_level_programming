#include "main.h"
/**
 * is_palindrome - function that checks if or not a string is palindrome
 * @s: string to return
 *
 * Return: 1 if a string is a palindrome and 0 if not.
 */
int is_palindrome(char *s)
{
	int s = is_palindrome;

	if (*S)
	{
		return ('\0');
	}
	else
	{
		return (0);
	}
}
